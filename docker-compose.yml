services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: coach
      POSTGRES_PASSWORD: coach
      POSTGRES_DB: coach
    ports:
      - "5439:5439"
    volumes:
      - coach_pgdata:/var/lib/postgresql/data
volumes:
  coach_pgdata: